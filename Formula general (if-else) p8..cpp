#include <stdio.h>
#include <conio.h>
#include <math.h>

main()
{
	int a,b,c;
	float x1,x2;
	printf("\tEl programa calculará las raíces con ayuda de la fórmula general.\n");
	printf("Proporciona el valor de a: ");
	scanf("%d", &a);
	printf("Proporciona el valor de b: ");
	scanf("%d", &b);
	printf("Proporciona el valor de c: ");
	scanf("%d", &c);
	if ((2*a)>0)
	{
	if((sqrt(pow(b,2)+4*a*c))<0)
	{
		printf("No se pueden calcular las raíces.");
	}
	else 
	{
		x1=(())
	}
}
